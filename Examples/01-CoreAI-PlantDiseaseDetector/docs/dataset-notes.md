# Dataset Notes

- This example is being repositioned for object detection, not simple image classification.
- A future detector pipeline should use a properly licensed plant disease dataset with bounding-box annotations and known provenance.
- `best.pt` is expected to come from a separate fine-tuning workflow and is not committed here.
- Large datasets should not be committed to this repository.
